---
title: "Quickstart"
description: "Get StablePay running on your OpenClaw agent in under 5 minutes"
icon: 'rocket'
---

## Before you start

You need:
- OpenClaw installed (`openclaw --version` shows ≥ 2026.3.1)
- A shell where you can set environment variables
- USDC in your wallet to pay for services; developers receive USDC directly to theirs

You do **not** need:
- Docker, Kubernetes, or any backend services (the gateway is already running)
- SOL for gas fees (covered by the platform hotwallet)
- Any prior blockchain knowledge

<Note>
  Production Mainnet payments are not yet deployed — the Sandbox gateway currently serves all traffic. API endpoints, authentication, and transaction flows are identical across both environments. Swap to Production values when Mainnet goes live. See [Environments](/api-reference/introduction#environments).
</Note>

## Step 1: Install the plugin

```bash
openclaw plugins install clawhub:stablepay-agentpay-dev
```

Verify it's loaded:

```bash
openclaw plugins list
```

Expected output includes `stablepay-agentpay-dev` with status `enabled`.

## Step 2: Configure OpenClaw

Add this to your `~/.openclaw/openclaw.json`:

<Tabs>
  <Tab title="Production">
    ```json
    {
      "plugins": {
        "entries": {
          "stablepay-agentpay-dev": {
            "enabled": true,
            "config": {
              "backendBaseUrl": "https://api.stablepay.co",
              "solanaRpcUrl": "https://api.mainnet-beta.solana.com",
              "splTokenMintAddress": "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",
              "owsRuntime": "auto",
              "didRegisterPath": "/api/v1/did/register",
              "requestTimeoutMs": 90000
            }
          }
        }
      },
      "tools": {
        "allow": [
          "stablepay_runtime_status",
          "stablepay_create_local_wallet",
          "stablepay_bind_existing_wallet",
          "stablepay_register_local_did",
          "stablepay_configure_payment_limits",
          "stablepay_sign_message",
          "stablepay_pay_via_gateway",
          "stablepay_query_balance",
          "stablepay_query_sales"
        ]
      }
    }
    ```
  </Tab>
  <Tab title="Sandbox">
    ```json
    {
      "plugins": {
        "entries": {
          "stablepay-agentpay-dev": {
            "enabled": true,
            "config": {
              "backendBaseUrl": "https://ai.wenfu.cn",
              "solanaRpcUrl": "https://api.devnet.solana.com",
              "splTokenMintAddress": "4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU",
              "owsRuntime": "auto",
              "didRegisterPath": "/api/v1/did/register",
              "requestTimeoutMs": 90000
            }
          }
        }
      },
      "tools": {
        "allow": [
          "stablepay_runtime_status",
          "stablepay_create_local_wallet",
          "stablepay_bind_existing_wallet",
          "stablepay_register_local_did",
          "stablepay_configure_payment_limits",
          "stablepay_sign_message",
          "stablepay_pay_via_gateway",
          "stablepay_query_balance",
          "stablepay_query_sales"
        ]
      }
    }
    ```
  </Tab>
</Tabs>

<Tip>
  Use Sandbox for development and testing — USDC has no real value, and you can obtain it from any Solana DevNet faucet. The full [environment reference](/api-reference/introduction#environments) lists every value that differs between the two.
</Tip>

## Step 3: Set environment variables

<Tabs>
  <Tab title="Production">
    ```bash
    # Required — encrypts your local wallet state
    export STABLEPAY_PLUGIN_MASTER_KEY="generate-a-long-random-secret-here"

    # Required — platform hotwallet public key
    export STABLEPAY_FEE_PAYER_SOL="<prod-hotwallet-pubkey>"

    # Optional — unattended OWS signing
    export STABLEPAY_OWS_PASSPHRASE="your-passphrase"
    ```
  </Tab>
  <Tab title="Sandbox">
    ```bash
    # Required — encrypts your local wallet state
    export STABLEPAY_PLUGIN_MASTER_KEY="generate-a-long-random-secret-here"

    # Required — Sandbox hotwallet public key
    export STABLEPAY_FEE_PAYER_SOL="FMNs7xqezz4bYYioPyfqPzxLLmZyJhjSzbGApMdnrC2Z"

    # Optional — unattended OWS signing
    export STABLEPAY_OWS_PASSPHRASE="your-passphrase"
    ```
  </Tab>
</Tabs>

<Tip>
  Add these to your `~/.zshrc` or `~/.bashrc` so they persist across sessions.
</Tip>

## Step 4: Restart and enter TUI

```bash
openclaw gateway restart
openclaw tui
```

## Step 5: Your first conversation

In the TUI, walk through these steps in order:

### 5.1 Check runtime status

> Run stablepay_runtime_status

Confirm the output shows `has_wallet: false` and an active driver is selected.

### 5.2 Create a wallet

> Run stablepay_create_local_wallet with user_id "alice", runtime "auto"

This creates a new Solana wallet via OWS. Record the `did` and `wallet_address` shown in the output.

### 5.3 Register your DID

> Run stablepay_register_local_did with user_type "agent"

This registers your public key with the StablePay backend. You now have a valid `did:solana:...` identity.

### 5.4 Set payment limits

> Run stablepay_configure_payment_limits with single_purchase_limit_usdc 100, auto_purchase_threshold_usdc 10

- **Single purchase limit**: Maximum per-transaction spend (hard cap)
- **Auto purchase threshold**: Purchases below this amount skip confirmation

### 5.5 Make a payment

> Run stablepay_pay_via_gateway with skill_did "did:solana:`<seller-address>`", skill_name "skill-name", price "1.00", currency "USDC"

If the payment succeeds, you'll get a `tx_id` back. The seller receives the USDC directly to their wallet.

<Tip>
  **Testing in Sandbox?** You can use the Sandbox hotwallet address as a dummy seller target: `did:solana:FMNs7xqezz4bYYioPyfqPzxLLmZyJhjSzbGApMdnrC2Z`. Sandbox USDC has no real value — spend freely during development.
</Tip>

### 5.6 Check your balance

> Run stablepay_query_balance with did "`<your-did>`"

## Next steps

<CardGroup cols={2}>
  <Card title="I'm building a paid service" icon="hammer" href="/integrating-payment">
    Learn how to integrate x402 payments into your skill or backend.
  </Card>
  <Card title="I want to publish on MoltBay" icon="store" href="/moltbay">
    List your service on the Agent marketplace.
  </Card>
  <Card title="Plugin config deep dive" icon="gear" href="/development">
    Every configuration option explained.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/introduction">
    Full REST API documentation.
  </Card>
</CardGroup>
